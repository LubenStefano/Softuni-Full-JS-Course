<section id="details-info">
  <h1>Details</h1>
  <div class="volcano-image">
    <img src="{{volcano.image}}" />
  </div>

  <div class="volcano-info">
    <div class="volcano-text">
      <h1 id="name">{{volcano.name}}</h1>
      <h3 id="location">Location: {{volcano.location}}</h3>
      <p id="type">Volcano Type: {{volcano.type}}</p>
      <p id="elevation"><span>Elevation: {{volcano.elevation}}</span></p>
      <p id="last-eruption">Year of Last Eruption: {{volcano.lastEruption}}</p>
      <p id="votes">Vote's Count: 2 TODO</p>
      <p id="description">{{volcano.description}}</p>
    </div>
    <div class="product-btn">
  <!-- Only for registered user and author of the publication -->
  {{#if isOwner}}
    <div class="author">
      <a href="/volcanoes/{{volcano._id}}/edit" class="btn-edit">Edit</a>
      <a href="/volcanoes/{{volcano._id}}/delete" class="btn-delete">Delete</a>
    </div>
  {{else}}
    <!-- Logged in user who is not the owner -->
    {{#if isUser}}
      {{#if userVoted}}
        <p class="vote-message">You've already voted for this volcano!</p>
      {{else}}
        <a href="/volcanoes/{{volcano._id}}/vote" class="btn-vote">Vote</a>
      {{/if}}
    {{/if}}
  {{/if}}
</div>
  </div>
</section>