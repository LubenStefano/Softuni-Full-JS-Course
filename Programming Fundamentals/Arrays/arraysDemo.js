function name() {
    let array = [1,2,3]
    // array.push(69)                           //ДОБАВЯ ОТЗАД : log -- >  (4) [1, 2, 3, 69]
    // array.unshift(69)                        //ДОБАВЯ ОТПРЕД: log -- > (4) [69, 1, 2, 3]
    // array.shift()                            //ВЗИМА ОТПРЕД: log -- > (2) [2, 3]
    // let buff = array.shift()                 //ВЗИМА И СЛАГА ОТПРЕД: buff ==> 1 [*1*,2,3] 
    // array.pop()                              //ВЗИМА ОТЗАД: log --> (2) [1, 2]
    // let buff = array.pop()                   //ВЗИМА И СЛАГА ОТЗАД: buff ==> 3 [1,2,*3*] 

    // array.includes(1)                        //СЪДЪРЖА ЛИ МАСИВА .includes(x) --> TRUE 
    // let joinArray = array.join("<->")        //ВСИЧКО СТАВА В ЕДИН СТРИНГ:  log--> "1<->2<->3"
    
    // console.log(joinArray);
    // console.log(buff);
}
name()

