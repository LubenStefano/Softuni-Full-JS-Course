function requiredReading(pages, pagesPerHour, day) {
    let hoursNeeded = (pages / pagesPerHour);
    let daysNeeded = hoursNeeded/day;
    console.log(daysNeeded);
}
requiredReading(212,

    20 ,
    
    2)